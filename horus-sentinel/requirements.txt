# ===== Core API =====
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ===== Orchestration & LLM =====
langgraph==0.2.28
langchain==0.3.1
langchain-community==0.3.1
httpx==0.27.2                 # async HTTP for Ollama + tools

# ===== Datastores =====
psycopg[binary]==3.2.2        # PostgreSQL
neo4j==5.24.0                 # Graph DB driver (optional — MVP uses networkx)
redis==5.0.8                  # cache / queue / rate budgets
chromadb==0.5.5               # RAG vector store (optional — MVP uses keyword retriever)
sqlalchemy==2.0.35            # ORM for relational models
alembic==1.13.2               # migrations

# ===== Knowledge graph (MVP, always required) =====
networkx==3.3                 # in-process Intelligence Knowledge Graph

# ===== OSINT / recon tools =====
dnspython==2.6.1
python-whois==0.9.4
shodan==1.31.0

# ===== Reporting =====
jinja2==3.1.4
weasyprint==62.3              # HTML -> PDF

# ===== Utilities =====
tenacity==9.0.0               # retries with backoff
structlog==24.4.0             # structured logging
